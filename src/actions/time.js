import { createAction } from 'redux-actions'

export const updateTime = createAction('TIME_UPDATE')
export const addTime = createAction('TIME_ADD')
export const removeTime = createAction('TIME_REMOVE')
